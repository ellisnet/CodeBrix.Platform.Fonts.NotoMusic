THIRD-PARTY SOFTWARE NOTICES AND INFORMATION

CodeBrix.Platform.Fonts.NotoMusic is a CodeBrix-family redistribution of the
Noto Music font, packaged as a .NET 10 NuGet library. Noto Music is a
musical-notation symbols font covering the Unicode music blocks — Western
musical symbols, Byzantine musical symbols and ancient Greek musical
notation — plus the miscellaneous-symbols music characters and a small set
of supporting Latin letters, digits and punctuation (no Greek or Cyrillic text glyphs). The package incorporates
one piece of third-party material:

  1. The Noto Music font (SIL OFL 1.1) — the single bundled font file.

The CodeBrix-authored parts of this package — the `.csproj`, the
`.ttf.manifest` JSON file, the `.uprimarker` marker, the documentation, and
the packaging metadata — are original to the CodeBrix family and contain no
third-party source code. They are released, together with the bundled font
file, under the SIL Open Font License, Version 1.1, the same license as the
font. The full license text is bundled with this repository at the
repository root — as `LICENSE` and as the byte-identical `OFL.txt` — and
`OFL.txt` is packaged inside the produced NuGet.

================================================================================

1. Noto Music Font
   ---------------
   Project URL:    https://github.com/notofonts/music
   Copyright:      Copyright 2022 The Noto Project Authors
                   (https://github.com/notofonts/music)
   License:        SIL Open Font License, Version 1.1

   Note on naming: the Noto Music copyright statement declares NO Reserved
   Font Name, so SIL OFL 1.1 condition 3 does not apply to this family. The
   `.ttf` file is redistributed bit-for-bit unmodified; the rename listed
   below changes the file name only, and the name tables inside the font are
   untouched.

   Files redistributed from this upstream project (binary `.ttf` files),
   as downloaded from Google Fonts
   (https://fonts.google.com/noto/specimen/Noto+Music):

     - src/CodeBrix.Platform.Fonts.NotoMusic/Fonts/NotoMusic.ttf

   Modifications made during redistribution:
     - The upstream `NotoMusic-Regular.ttf` was renamed to `NotoMusic.ttf`,
       matching the dash-free naming convention the CodeBrix font packages
       use for fonts that must always be present (the consumer-build prune
       in the sibling packages removes only dash-bearing file names). No
       `.ttf` binary content was altered.
     - Noto Music publishes exactly one face upstream — Regular weight,
       upright, Normal stretch, static (no variable font) — so this package
       ships that one file and nothing was omitted.

================================================================================

License statement
-----------------

The combined work is published under the SPDX expression `OFL-1.1`. SIL OFL
1.1 is a permissive font license that explicitly allows fonts to be bundled
and redistributed alongside any other software (condition 2). The
CodeBrix-authored wrapper and packaging metadata are released under the same
SIL OFL 1.1 as the bundled font file.

Jeremy Ellis and contributors license the CodeBrix.Platform.Fonts.NotoMusic
project to you under the terms set forth in the LICENSE file (SIL OFL 1.1),
located at the root of this repository; the same license text is carried in
OFL.txt with the upstream copyright header.

If you have any questions about any third-party code or assets referenced
here, or if you believe that any third-party material has been included in
CodeBrix.Platform.Fonts.NotoMusic in error, please open a new issue at:
https://github.com/ellisnet/CodeBrix.Platform.Fonts.NotoMusic/issues

================================================================================

END OF THIRD-PARTY SOFTWARE NOTICES AND INFORMATION
